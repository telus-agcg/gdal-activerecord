source 'https://rubygems.org'

# Specify your gem's dependencies in gdal-activerecord.gemspec
gemspec
gem 'ffi-gdal-extensions', path: '../ffi-gdal-extensions'

# The gem doesn't _depend_ on these, but they're nice to have around
group :test, :development do
  gem 'byebug'
  gem 'rubocop'
end
